==========
Controller
==========

initialize
==========

If an initialize method exists on the controller it will be called after the constructor has executed. If it returns a promise the view will not be loaded until the promise has resolved.